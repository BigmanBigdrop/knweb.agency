// Quick metadata additions for existing pages
import type { Metadata } from "next";
import {
  servicesMetadata,
  offresMetadata,
  projetsMetadata,
  aproposMetadata,
  contactMetadata,
  blogMetadata,
} from "@/lib/metadata";

// Export metadata objects for easy import in page files
export {
  servicesMetadata,
  offresMetadata,
  projetsMetadata,
  aproposMetadata,
  contactMetadata,
  blogMetadata,
};
